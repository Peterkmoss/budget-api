import axios from 'axios'
import jwt from 'jsonwebtoken'
import dotenv from 'dotenv'
dotenv.config()
describe('Budgets tests', () => {
    it('Runs this test', () => {
        expect(0).toBe(0)
    })
})